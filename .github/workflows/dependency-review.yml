# Disabled: Requires Dependency Graph to be enabled in repository settings
# To enable: Go to repository Settings > Security & analysis > Enable Dependency graph
# name: Dependency Review

# on:
#   pull_request:
#     branches: [ main, master ]

# permissions:
#   contents: read

# jobs:
#   dependency-review:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Dependency Review
#         uses: actions/dependency-review-action@v4
#         with:
#           fail-on-severity: moderate
